# Frontend

This directory contains the React and Vite frontend for the Cognitensor local dApp.

For the complete blockchain, deployment, MetaMask, and startup instructions, see the [project README](../README.md).

To run only the frontend after the contract has been deployed:

```bash
npm install
npm run dev
```
